app:
  env: "dev"
  port: "50052"
  log_level: "debug"
  service_name: "database_init_service"

database:
  url: "postgres://postgres:password@postgres:5438/pure?sslmode=disable"
  migrations_path: "./deploy/migrations"
